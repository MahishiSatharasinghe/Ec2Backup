apiVersion: v1
kind: Service
metadata:
  name: analytics-service
spec:
  type: NodePort
  selector:
    app: analytics-service
  ports:
    - port: 8003
      targetPort: 8003
      nodePort: 32173  # ✅ Public port to use in tracker.js

